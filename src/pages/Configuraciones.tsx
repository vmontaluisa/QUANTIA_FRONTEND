import React from 'react';
import { Typography } from '@mui/material';

const Configuraciones = () => {
  return <Typography variant="h5">Configuraciones Generales</Typography>;
};

export default Configuraciones;
